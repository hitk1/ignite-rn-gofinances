import React from 'react'
import { View, Text } from 'react-native'

import {
    Container
} from './styles'

export const Dashboard = () => {
    return (
        <Container>
            <Text>Dashboard</Text>
        </Container>
    )
}